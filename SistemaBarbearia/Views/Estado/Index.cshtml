@model IEnumerable<SistemaBarbearia.Models.Estados.Estado>


<head>

</head>
<body>
    <div class="container ">
        <h2 class="container-fluid">Consultar Estado</h2>
        <br />
        <div class="row">
            <div class="col-md-6">
                <div class="form-group">
                    @Html.TextBox("filter", null, new { @class = "form-control col-md-12", maxlength = "100", @placeholder = "Pesquisar...", @autofocus = "autofocus" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-md-4">
                    @Html.ActionLink("Buscar", "", routeValues: null, htmlAttributes: new { @class = "btn btn-success ", @role = "button" })

                </div>
            </div>
            <div class="form-group">
                <div class="col-md-4">
                    @Html.ActionLink("Novo", "Create", routeValues: null, htmlAttributes: new { @class = "btn btn-primary", @role = "button" })
                </div>
            </div>
        </div>
        <table id="example" class="table table-sm table-dark table-striped table-bordered dt-responsive nowrap">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Estado</th>
                    <th>Data Cadastro</th>
                    <th>Opções</th>
                </tr>
            </thead>
            @*<tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Id)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.nmEstado)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.dtCadastro)
                        </td>
                        <td>
                            @Html.ActionLink("Alterar", "Edit", new { id = item.Id }) |
                            @Html.ActionLink("Visualizar", "Details", new { id = item.Id }) |
                            @Html.ActionLink("Excluir", "Delete", new { id = item.Id })
                        </td>
                    </tr>
                }
            </tbody>*@
        </table>
    </div>
</body>
